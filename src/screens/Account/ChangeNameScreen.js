import { Text } from 'react-native-elements'
import { SafeAreaView } from 'react-native-safe-area-context'

export const ChangeNameScreen = () => {
    return <>
        <SafeAreaView>
            <Text>Cambiar el nombre de la cuneta</Text>
        </SafeAreaView>
    </>
}