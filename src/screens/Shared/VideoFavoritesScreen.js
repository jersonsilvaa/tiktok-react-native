import { SafeAreaView } from 'react-native-safe-area-context'
import { Text } from 'react-native-elements'

export const VideoFavoritesScreen = () => {
    return <>
        <SafeAreaView>
            <Text>VideoFavoritesScreen</Text>
        </SafeAreaView>
    </>
}