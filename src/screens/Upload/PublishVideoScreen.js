import { Text } from 'react-native-elements'
import { SafeAreaView } from 'react-native-safe-area-context'

export const PublishVideoScreen = (props) => {
  console.log(props)
  return <>
    <SafeAreaView>
      <Text>Publicar video</Text>
    </SafeAreaView>
  </>
}