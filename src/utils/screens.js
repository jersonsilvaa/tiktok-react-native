const app = {
    tab: 'Tab'
}

const auth = {
    auth: 'auth',
    loginEmail: 'Iniciar sesión',
    registerEmail: 'Registrarse'
}

const home = {
    tab: 'Inicio'
}

const friends = {
    tab: 'Amigos'
}

const upload = {
    tab: 'Subir vídeo'
}

const notifications = {
    tab: 'Notificaciones'
}

const account = {
    tab: 'Perfil'
}

export const screen = {
    auth,
    app,
    home,
    friends,
    upload,
    notifications,
    account
}